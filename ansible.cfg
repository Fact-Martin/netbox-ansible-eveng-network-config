[defaults]
# Silently convert invalid group characters (like hyphens) to underscores
force_valid_group_names = silently

# Optional: Clean up output by hiding the deprecation warnings you see at the top
deprecation_warnings = False
